%===============================================================
% _._     _,-'""`-._
% (,-.`._,'(       |\`-/|
%     `-.-' \ )-`( , o o)
%           `-    \`_`"'-

% __..--''``---....___   _..._    __
% /// //_.-'    .-/";  `        ``<._  ``.''_ `. / // /
% ///_.-' _..--.'_    \                    `( ) ) // //
% / (_..-' // (< _     ;_..__               ; `' / ///
% / // // //  `-._,_)' // / ``--...____..-' /// / //
% Header and Style File by Damanic
% Will contain comments since this is a learning process for me
%===============================================================

% 1. 
\ProvidesPackage{header}
%===============================================================
% Packages Used
%===============================================================
\usepackage{amsmath}
\usepackage{circuitikz}
\usepackage{enumitem}
\usepackage{lipsum}
\usepackage{float}
\usepackage{geometry}
\usepackage{graphicx}
\usepackage{hyperref}
\usepackage[parfill]{parskip}
\usepackage{pdfpages}
\usepackage{pgfplots}
\usepackage{setspace}
\usepackage{tikz}
\usepackage{tocbasic}
\usepackage{tocloft}
\usepackage{tcolorbox}
\usepackage{upgreek}
\usepackage[utf8]{inputenc}
\usepackage{xcolor}
%package used for delta wye 
\usepackage{rotating,stackengine,scalerel}
\geometry{
    a4paper,
    total={170mm,257mm},
    left=20mm,
    top=20mm,
}
%===============================================================
% Data
%===============================================================
\def\author{Damanic Luck}

%2. 
\newcommand{\semester}[1]{\newcommand{\sem}{#1}}

% Following environments are for emphasis within the document
\newenvironment{define}
    {\begin{tcolorbox}[
        title=Definition,
        colback=pink!5!white,
        colframe=pink!100!black
        ]}
    {\end{tcolorbox}}

\newenvironment{concept}
    {\begin{tcolorbox}[
        title=Concept,
        colback=blue!5!white,
        colframe=blue!50!white
        ]}
    {\end{tcolorbox}}

\newenvironment{sanity}
    {\begin{tcolorbox}[
        title=Sanity Check,
        colback=red!5!white,
        colframe=red!70!white
        ]}
    {\end{tcolorbox}}

% copied from : https://tex.stackexchange.com/questions/481532star-wye-electrical-connection-math-symbol
\newcommand\wye{\scalerel*{\stackengine{-1pt}{%
  \rotatebox[origin=c]{30}{\rule{10pt}{.9pt}}\kern-1pt%
  \rotatebox[origin=c]{-30}{\rule{10pt}{1.3pt}}}{%
  \rule{.9pt}{10pt}}{O}{c}{F}{F}{S}}{\Delta}}

%===============================================================
% Additional Notes, Applications, Main Walkthrough styling
%===============================================================

%===============================================================
% Notes with the appropriate numbering, like footnotes
% 1. \usepackage can only be used in the document preamble while here I'm making my own package for my own use essentially?
% 2. Follows the following structure of \newcommand{cmd}[args]{def} or \renewcommand{cmd}[args]{def}
    % cmd 
    %     A command name beginning with a \. For \newcommand it must not be already     defined and must not begin with \end; for \renewcommand it must already be defined.
    % args
    %     An integer from 1 to 9 denoting the number of arguments of the command being defined. The default is for the command to have no arguments.
    % def 
    %     The text to be substituted for every occurrence of cmd; a parameter of the form #n in cmd is replaced by the text of the nth argument when this substitution takes place.
%===============================================================
